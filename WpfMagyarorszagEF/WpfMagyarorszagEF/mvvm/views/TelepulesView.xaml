<Window x:Class="WpfMagyarorszagEF.mvvm.views.TelepulesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfMagyarorszagEF.mvvm.views"
        mc:Ignorable="d"
        Title="Magyarország települései" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="9*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <DataGrid ItemsSource="{Binding Telepulesek}" AutoGenerateColumns="False" ColumnWidth="*" SelectedItem="{Binding SelectedTelepules}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Irányítószám" Binding="{Binding Irszam}" />
                <DataGridTextColumn Header="Név" Binding="{Binding Nev}" />
                <DataGridTextColumn Header="Terület" Binding="{Binding Terulet}" />
                <DataGridTextColumn Header="Népesség" Binding="{Binding Nepesseg}" />
                <DataGridTextColumn Header="Jogállás" Binding="{Binding JogallasNavigation.Jogallas}" />
                <DataGridTextColumn Header="Megye" Binding="{Binding MegyekodNavigation.Nev}" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
