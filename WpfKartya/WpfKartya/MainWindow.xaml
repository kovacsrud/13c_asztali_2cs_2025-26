<Window x:Class="WpfKartya.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfKartya"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Title="MainWindow" Height="450" Width="800">
    <Window.Background>
        <SolidColorBrush Color="{StaticResource kozepkek}" Opacity="0.3" />
    </Window.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Rectangle x:Name="rectHeader" Grid.ColumnSpan="3" RadiusX="5" RadiusY="5" MouseDown="rectHeader_MouseDown">
            <Rectangle.Fill>
                <SolidColorBrush Color="{StaticResource sotetkek}" />
            </Rectangle.Fill>
        </Rectangle>
        <WrapPanel Grid.ColumnSpan="3" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <fa:FontAwesome x:Name="minGomb" Icon="WindowMinimize" Margin="0,5,5,0" FontSize="40" Foreground="{StaticResource kozepkekBrush}" MouseDown="minGomb_MouseDown"/>
            <fa:FontAwesome x:Name="maxGomb" Icon="WindowMaximize" Margin="0,5,5,0" FontSize="40" Foreground="{StaticResource kozepkekBrush}" MouseDown="maxGomb_MouseDown"/>
            <fa:FontAwesome x:Name="closeGomb" Icon="WindowClose" Margin="0,5,5,0" FontSize="40" Foreground="{StaticResource kozepkekBrush}" MouseDown="closeGomb_MouseDown"/>
        </WrapPanel>
        <Image x:Name="randomKartya" Source="{Binding SelectedKartya.KartyaKep}" Grid.Column="0" Grid.Row="2" Grid.RowSpan="6" Margin="5"/>
        <Image x:Name="kartyaBack" Source="{Binding SelectedHatter}" Grid.Column="1" Grid.Row="2" Grid.RowSpan="6" Margin="5"/>
        <Button Style="{DynamicResource GombStyle1}" Foreground="{StaticResource vilagosBrush}" x:Name="buttonPiros" Content="Piros" Grid.Column="2" Grid.Row="5" Width="200" Height="30" Click="buttonPiros_Click"/>
        <Button Style="{DynamicResource GombStyle1}" Foreground="{StaticResource vilagosBrush}" x:Name="buttonFekete" Content="Fekete" Grid.Column="2" Grid.Row="6" Width="200" Height="30" Click="buttonFekete_Click"/>
    </Grid>
</Window>
